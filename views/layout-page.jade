extends layout

block main
    #messages.messages
    if page && page.deleted
        .alert.alert-warning= __("page-status-deleted")
    .row
        if (page.path !== '/')
            .col-md-10
                block content

            .col-md-2
                block pagemeta
        else
            .col-md-12
                block content