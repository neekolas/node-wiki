extends layout

block main
    div.navbar
        div.navbar-inner
            a(href="#").navbar-brand= __("all-pages")
    ul
        each page in pages
            li.preview
                a(href=page.path)
                    img(src="#{page.image}").img-polaroid
                    p= page.title
